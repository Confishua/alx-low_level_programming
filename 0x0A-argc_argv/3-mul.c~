#include <stdio.h>

/*
*main- program multiplies two numbers and return the result
*
*return: nothing
*/

int main(void)
{
int i = 2;
int j = 20;
int result;
result = (i * j);
printf("%d\n", result);
printf("\n");
return (0);
}
