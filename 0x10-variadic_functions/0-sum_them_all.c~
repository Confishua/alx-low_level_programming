#include <stdarg.h>
#include <stdio.h>


int sum_them_all(const unsigned int n, ...);

int val = 0;
va_list ap;
unsigned int n;

va_start(ap, num_args);
for(i = 0; i < num_args; i++) {
val += va_arg(ap, const unsigned int n);
}
va_end(ap);
if ( n == 0)
{
return 0
}
   return val;
}
