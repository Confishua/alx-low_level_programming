#include <string.h>
#include "main.h"

/**
*_strlen_recursion - displays the length of the string
*@s: The character whose string would be displayed
*Return: 0 on success
*/

int _strlen_recursion(char *s)
{
return (strlen(s));
}

